apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: cert-manager-https-route
  namespace: agc-lab
spec:
  parentRefs:
    - name: agc-lab-gateway
      sectionName: https
  hostnames:
    - "${LAB_DOMAIN}"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: web-v1
          port: 8080
