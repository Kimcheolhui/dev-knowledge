apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: route-01
  namespace: test-infra
spec:
  parentRefs:
  - name: gateway-01
  rules:
  - backendRefs:
    - name: backend-v1
      port: 8080
